import{getOption}from"../ctxpage/conf-options.js";import{toFormattedNumber}from"./numbers.js";import{translate}from"./translate.js";function standardUnit(amount,customTradeRate=null,disableUnitMode=false){if(!Array.isArray(amount))return;const standardUnitBase=getOption("standardUnitBase");const tradeRate=!Array.isArray(customTradeRate)?getOption("tradeRate"):customTradeRate;const tradeBase=disableUnitMode?0:standardUnitBase;let standardUnitValue=0;[0,1,2].forEach((id=>{standardUnitValue+=(amount[id]||0)/tradeRate[id]*tradeRate[tradeBase<0?id:tradeBase]}));return standardUnitValue}function unitType(full=false){const standardUnitBase=Number(getOption("standardUnitBase"));if(full)return translate(178+standardUnitBase);else return translate(174+standardUnitBase)}export{standardUnit,unitType};